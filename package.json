{
  "name": "@composition/auth",
  "version": "0.0.1-beta.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "prettier-standard src/**/*.js",
    "test": "npm run lint",
    "preversion": "npm run test",
    "postversion": "git push && git push --tags && [ -z \"$(node -p 'require(`./package.json`).version' | grep '-')\" ] && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cookie-parser": "^1.4.4",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "passport": "^0.4.0",
    "passport-google-oauth20": "^2.0.0",
    "passport-jwt": "^4.0.0"
  },
  "devDependencies": {
    "prettier-standard": "^9.1.1"
  }
}
